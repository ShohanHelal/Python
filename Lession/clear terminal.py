import os
while True : 
    os.system('cls') # this is the clear for window. it work in this loop
    a = input("Enter Your Name : ")
    print(a)
    input("Press Enter to Continue!!!")# if we don't type this . the system will clear without printing anything
    #for both window and linux 'os.system('cls' if os.neme == 'nt' else 'clear')'